[project]
name = "mozaops-libs"
version = "0.0.0"
description = "Utilitários técnicos partilhados entre serviços — vazio até ao segundo consumidor de closing-reconciliation."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mozaops_libs"]
