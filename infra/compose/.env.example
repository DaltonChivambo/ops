# Copiar para .env nesta pasta. Valores de desenvolvimento.
ENVIRONMENT=development
LOG_LEVEL=INFO

TICKETING_GRAFANA_WEBHOOK_TOKEN=token_dev

SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_FROM=ops@exemplo.local
